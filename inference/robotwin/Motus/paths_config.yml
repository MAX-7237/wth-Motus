# Motus Evaluation Path Configuration
# Paths needed for inference

# ============================================================================
# Core Paths - MUST MODIFY THESE
# ============================================================================
robotwin_root: "/mnt/gyc/RoboTwin"     # e.g., "/path/to/RoboTwin"
conda_env: "/mnt/gyc/miniconda3/envs/robotwin_wth"         # e.g., "/path/to/miniconda3/envs/RoboTwin"
checkpoint_path: "/mnt/gyc/wth-Motus/pretrained_models/Motus_robotwin2"   # (directory containing mp_rank_00_model_states.pt)

# Pretrained model paths (only for loading configs, not weights)
wan_path: "/mnt/gyc_ckp/Wan2.2-TI2V-5B"          # e.g., "/path/to/Wan2.2-TI2V-5B"
vlm_path: "/mnt/gyc/wth-Motus/pretrained_models/Qwen3-VL-2B-Instruct"    # e.g., "/path/to/Qwen3-VL-2B-Instruct"

# ============================================================================
# Optional Configuration
# ============================================================================
# GPU IDs (empty means auto-detect)
gpu_ids: []  # e.g., [0, 1, 2, 3, 4, 5, 6, 7]

# Task configuration
task_config: "demo_randomized"
seed: 42
tasks_file: "tasks_all.txt"
